version: '3.4'

services:
  Wetracked.com:
    image: Wetracked.com
    build:
      context: .
      dockerfile: /Dockerfile
    ports:
      - 8000:8000
    redis:
       image: redis:listest
    db:
       image: mongo:listest
