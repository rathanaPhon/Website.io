function showreview(url){
        const xhr = new XMLHttpRequest();
        log.content = $(log)
            xhr.onload = () => {
            console.log(xhr.responseXML.title);
             URL('http://rathanaphon.github.io/webpage/')
        };
       xhr.open("GET", "file.html");
       xhr.responseType = "document";
       xhr.send();  
   }

